<mod>
    <ffi>
        <name>calc_power</name>
        <dll>
            <str>mathdll.dll</str>
        </dll>
        <func>
            <str>power</str>
        </func>

        <args>
            <!-- base -->
            <f64 />
            <!-- exp -->
            <i32 />

            <!-- res -->
            <f64 ref />
        </args>

        <returns>
            <void />
        </returns>
    </ffi>

    <export>
        <calc_power />
    </export>
</mod>